numpy>=1.21
pandas>=1.3
torch>=1.12
tqdm>=4.65
scikit-learn>=1.1
tensorboard>=2.9
pyyaml>=6.0
pyyaml-safe-loader>=1.0.1
pytest>=7.1
pytest-cov>=3.0
black>=22.3
sentence-transformers==5.3.0  # pinned for embedding reproducibility — see LLM-rec/config/generation_config.yaml
huggingface_hub>=0.20         # InferenceClient for generative summarization (HF backend)
ollama>=0.1                   # local generative backend (no API key needed)
python-dotenv>=1.0            # auto-loads .env so HF_TOKEN doesn't need manual export
matplotlib>=3.5